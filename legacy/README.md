# Legacy Scripts

These files are the original root-level scripts retained for historical
comparison while the maintained Python port lives under `slam/` and
`examples/`.

Run new work through the chapter examples instead of extending these scripts.
